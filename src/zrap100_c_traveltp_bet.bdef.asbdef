projection implementation in class ZRAP100_BP_C_TRAVELTP_BET unique;
strict ( 2 );
use draft;
define behavior for ZRAP100_C_TRAVELTP_BET alias Travel
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action deductDiscount;
  use action copyTravel;

  use action acceptTravel;
  use action rejectTravel;
}